{
  "routes": [
    {
      "method": "GET",
      "path": "/addresses",
      "handler": "Addresses.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/addresses/count",
      "handler": "Addresses.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/addresses/:id",
      "handler": "Addresses.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/addresses",
      "handler": "Addresses.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/addresses/:id",
      "handler": "Addresses.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/addresses/:id",
      "handler": "Addresses.destroy",
      "config": {
        "policies": []
      }
    }
  ]
}
